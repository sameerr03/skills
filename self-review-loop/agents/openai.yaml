interface:
  display_name: "Self Review Loop"
  short_description: "Run an adversarial review and fix loop"
  default_prompt: "Use $self-review-loop to adversarially review this implementation, fix valid findings, and repeat only when warranted."
